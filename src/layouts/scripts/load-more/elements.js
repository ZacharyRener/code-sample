/**
 * The elements.
 *
 * @module zach/blocks/load-more
 */

/** The load more button objects, if they exist. */
const loadMoreButtons = document.querySelectorAll('.zach-load-more-button');

export {
	/**
	 * The query objects for the loadmore button.
	 */
	loadMoreButtons,
};
